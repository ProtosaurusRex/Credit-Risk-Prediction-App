# Project dependencies (pinned to versions from the project's .venv where available)
xgboost==3.1.2
scikit-learn==1.8.0
pandas==2.3.3
joblib==1.5.3
numpy==2.4.0
scipy==1.16.3
matplotlib==3.10.8
seaborn==0.13.2
notebook==7.5.1
ipykernel==7.1.0
# Streamlit required to run the app; install the latest compatible version
streamlit

# If you need to pin streamlit to a specific version, replace the line above
# with e.g. streamlit==1.30.0
